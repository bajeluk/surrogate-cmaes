function generateReport(folder)
  % generates report file from experiment in FOLDER
  
  if nargin == 0
    help generateReport
    return
  end
  
  % load results
  % count EFE
  % compare EFE
  % print to file
end